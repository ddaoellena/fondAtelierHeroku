<div class="container">
	<%= form_for :news, url:news_index_path(@new),  method: :patch do |f| %>

	<div>
	<%= f.label :titre, "Titre *" %>
	<%= f.text_field :titre, {class: "form-control input-lg"} %>
	</div><br>

	<div>
	<p>
    <%= f.label :contenu, "Contenu *" %><br>
    <%= f.text_area :contenu,  {class: "form-control input-lg"}%>
 	</p>
	</div><br>

	<%= f.submit "Créer", class:"btn btn-primary" %>
	<%end%>
</div>